//! Provider implementations for different cloud platforms
//!
//! This module is reserved for future provider trait implementations.
//! Currently, the CLI uses direct implementations in `aws.rs`, `runpod.rs`, etc.
//!
//! See `src/provider.rs` for the `TrainingProvider` trait definition.
